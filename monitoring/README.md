# Monitoring - Prometheus and Grafana

Prometheus collects metrics such as CPU, memory, pod status, request count and errors.

Grafana creates dashboards from Prometheus metrics.

In real DevOps work:
- Prometheus = metrics collection
- Grafana = visualization and alerts

Example:
If CPU reaches 90%, Grafana can show an alert before users complain.
